# 🚀 Практика 2: Микросервисный чат

## 📋 Описание
Вы получили микросервисное приложение чата, состоящее из frontend, backend, Redis и Nginx. Ваша задача - правильно собрать и запустить все компоненты используя Docker.

## 📦 Что дано
```
starter-code/
├── backend/                      # Node.js API (нужен Dockerfile)
│   ├── server.js
│   └── package.json
├── configs/                      # Примеры конфигов
│   └── nginx.conf.example
└── docker-compose.yml.template   # Шаблон с пропусками
```
### 🐳 Готовые образы Frontend в Docker Hub
Доступны 3 версии frontend как готовые Docker образы:
- `prostodevops/chat-frontend:v1`
- `prostodevops/chat-frontend:v2`
- `prostodevops/chat-frontend:v3`

## ⚠️ ВАЖНО: Совместимость версий

Не все версии frontend и backend совместимы между собой!

| Frontend образ | Backend версия (ENV) | Работает? |
|---------------|---------------------|-----------|
| `prostodevops/chat-frontend:v1` | `BACKEND_VERSION=v1` | ✅ |
| `prostodevops/chat-frontend:v1` | `BACKEND_VERSION=v2` | ❌ |
| `prostodevops/chat-frontend:v1` | `BACKEND_VERSION=v3` | ❌ |
| `prostodevops/chat-frontend:v2` | `BACKEND_VERSION=v1` | ❌ |
| `prostodevops/chat-frontend:v2` | `BACKEND_VERSION=v2` | ❌ |
| `prostodevops/chat-frontend:v2` | `BACKEND_VERSION=v3` | ✅ |
| `prostodevops/chat-frontend:v3` | `BACKEND_VERSION=v1` | ❌ |
| `prostodevops/chat-frontend:v3` | `BACKEND_VERSION=v2` | ✅ |
| `prostodevops/chat-frontend:v3` | `BACKEND_VERSION=v3` | ❌ |

## 📝 Задания

### 1. Создайте Dockerfile для backend
- Базовый образ: `node:18-alpine`
- Установите зависимости из `package.json`
- Откройте порт 3000
- Запустите сервер

### 2. Допишите docker-compose.yml
Заполните пропуски в шаблоне:
- **frontend**: используйте готовый образ из Docker Hub (выберите версию)
- **backend**: используйте свой Dockerfile, установите версию через ENV
- **redis**: используйте официальный образ
- **nginx**: настройте реверс-прокси
- Создайте общую сеть `chat-network`
- Настройте volumes для Redis персистентности

### 3. Настройте nginx.conf
Создайте конфигурацию для проксирования:
- `/` → frontend (порт 80 внутри контейнера)
- `/api` → backend (порт 3000)
- `/socket.io` → backend с поддержкой WebSocket

### 4. Выберите правильные версии
- Попробуйте разные версии frontend образа (`v1`, `v2`, `v3`)
- Установите соответствующую версию backend через `BACKEND_VERSION`
- Найдите рабочую комбинацию

## ✅ Критерии успеха
- [ ] Чат открывается на http://localhost
- [ ] Отображаются версии frontend и backend
- [ ] Можно отправлять сообщения
- [ ] Сообщения видны в real-time всем пользователям
- [ ] История сообщений сохраняется после перезапуска
- [ ] Нет ошибки "Version mismatch!"

## 🏗 Структура решения

### Создаем папку solution и дублируем все туда 

```
solution/
├── backend/
│   └── Dockerfile         # Ваш Dockerfile для backend
├── nginx/
│   └── nginx.conf        # Ваша конфигурация nginx
└── docker-compose.yml    # Полный файл композиции
```

## 💡 Подсказки
- Redis нужен volume для персистентности данных между перезапусками
- В nginx.conf нужна поддержка WebSocket для socket.io

## 🧪 Проверка
```bash
cd solution/
# Копируем исходники backend (для сборки)
cp -r ../starter-code/backend .

# Запускаем все сервисы
docker-compose up --build

# Проверяем:
# 1. Откройте http://localhost
# 2. Должны видеть версии (например: Frontend: v2 | Backend: v3)
# 3. Отправьте сообщение
# 4. Откройте в другом браузере - сообщения должны быть видны
# 5. Перезапустите docker-compose - история должна сохраниться
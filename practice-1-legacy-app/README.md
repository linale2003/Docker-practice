# 🚀 Мини-проект 1 : Docker1.1

## 📋 Описание
Упаковка legacy приложения погоды с помощью docker-compose


## 🧪 Проверка
```bash
cd solution/
# Копируем исходники приложения
cp -r ../broken-weather-app/* .

# Запускаем все сервисы
docker-compose up --build

# В другом терминале:
# 1. Инициализируем БД (если не сделано автоматически)
docker-compose exec app python init_db.py

# 2. Проверяем health endpoint
curl http://localhost:5000/health
# Ожидаемый ответ: {"app":"ok","database":"ok","redis":"ok"}

# 3. Открываем в браузере
# http://localhost:5000
# Вводим город и проверяем работу
```
